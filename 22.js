var helpers = require("./helpers");

const part1 = (data) => {
  const lines = helpers.splitByNewLine(data);
  return 1;
};

const part2 = (data) => {
  const lines = helpers.splitByNewLine(data);
  return 2;
};

module.exports = {
  part1,
  part2,
};
